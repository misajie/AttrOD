# Study-area configs

YAML stubs only. **Do not invent study-area city lists** — Table 1 / zone clips are filled when areas pass inclusion (`N≥25`, `h_intra≤0.70` on unsliced weekday total).

Copy `_template.yaml` per area and set S1 fields from `../s1_schema.yaml`.
